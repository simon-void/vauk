import javax.crypto.SecretKey

class VauEncryptionService {
    fun symmetricEncrypt(plainBytes: ByteArray, key: SecretKey): ByteArray {
        TODO()
    }

    fun symmetricDecrypt(encodedBytes: ByteArray, key: SecretKey): ByteArray {
        TODO()
    }

    fun asymmetricEncrypt(plainBytes: ByteArray, key: SecretKey): ByteArray {
        TODO()
    }

    fun asymmetricDecrypt(encodedBytes: ByteArray, key: SecretKey): ByteArray {
        TODO()
    }
}